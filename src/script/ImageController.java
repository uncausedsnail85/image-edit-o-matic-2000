package script;

import images.ImageModel;

/**
 * Represents a Controller for the image program. Handles user inputs to handle images, includiing
 * loading, saving and filtering the images.
 */
public interface ImageController {
  /**
   * Gives the control over to the controller and executes the program flow.
   */
  void go();
}
